
<view class="all-order">
<input class="search" placeholder="请输入" />
<!--tab switch-->
<view class="tab">
  <view class="switch" wx:for="{{tab}}" class="{{ (ac == 1)? a : b }}" >
  {{item.name}}( {{ item.num }} )
  </view>
</view>

<!--循环操作仓库-->
<view class="classname"></view>
</view>